<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Movie Edit Page</title>
  </head>
  <body>
    <h1>Movie Edit - <%= movie.name %></h1>
    <form method="POST" action="/movies/<%=movie._id%>?_method=PUT">
      <div>
        <label for="name">Movie Name:</label>
        <input type="text" name="name" id="name" value="<%= movie.name %>" />
      </div>
      <div>
        <label for="isReadyToEat">Is Ready to Watch:</label>
        <input type="checkbox" name="isReadyToWatch" id="isReadyToWatch" <%=
        fruit.isReadyToWatch ? "checked" : "" %> />
      </div>

      <button type="submit">Edit Movie</button>
    </form>
  </body>
</html>